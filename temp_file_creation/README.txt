This folder is used as a template by app.py. 

Its copies are used whenever predictions are made or MOF components are identified. Also, when latent space neighbor information is requested.

__init__.py files purely used for Git to recognize empty folders.